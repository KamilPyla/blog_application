<%= form_with(model: product_sub_category) do |form| %>
  <% if product_sub_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(product_sub_category.errors.count, "error") %> prohibited this product_sub_category from being saved:</h2>

      <ul>
        <% product_sub_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :parent_category_id, style: "display: block" %>
    <%= form.text_field :parent_category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
